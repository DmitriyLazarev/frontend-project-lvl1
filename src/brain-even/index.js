import greetingAndIntroduction from '../cli';

const name = greetingAndIntroduction();

const brainEvenGame = (name) => {
    console.log('Answer "yes" if the number is even, otherwise answer "no".');
}
